# 9:16 Visual / Capture Plan

Target canvas: **1080×1920 vertical**, 30 fps. All visuals can be captured from the public RustChain source or rendered as original text cards; no third-party footage is required.

| Time | Narration beat | Exact visual |
|---|---|---|
| 0:00–0:04 | “Most blockchains…” | Black background. Large centered text: `OLD HARDWARE > FAST HARDWARE?` then smaller `RustChain RIP-200`. No stock footage. |
| 0:04–0:13 | VAX 3.5x base multiplier | Screen-record the pinned GitHub source `node/rip_200_round_robin_1cpu1vote.py`. Search for `"vax": 3.5`. Crop tightly around the code entry. Add a non-destructive callout: `BASE ANTIQUITY MULTIPLIER: 3.5x`. |
| 0:13–0:23 | Not guaranteed earnings | Original text card: `3.5x BASE ≠ 3.5x GUARANTEED PAYOUT`. Below it animate three labels in order: `time aging` → `attestation / eligibility` → `weighted epoch allocation`. |
| 0:23–0:34 | Round-robin block production | Screen-record the file header showing `RIP-200: Round-Robin Consensus (1 CPU = 1 Vote)` and the key-change text `Deterministic rotation (no lottery)`. Overlay: `BLOCK TURNS ≠ REWARD WEIGHT`. |
| 0:34–0:43 | Separate participation from weighting | Simple original diagram: left lane `BLOCK PRODUCTION` → `round-robin`; right lane `REWARD SHARE` → `time-aged weight`. Join at bottom with `same protocol, different jobs`. |
| 0:43–0:50 | Public code / close | Return to the `"vax": 3.5` code crop. Final card: `Vintage silicon is an input to the economics.` Footer: `Source: Scottcjn/Rustchain · RIP-200`. |

## Capture instructions

1. Open the pinned source URL from `SOURCES.md`, not a mutable branch view.
2. Use browser zoom 175–200% so the relevant code is legible in a vertical crop.
3. Keep the repository/path and enough surrounding code visible to prove provenance.
4. Do not show wallet balances, API keys, private tabs, notifications, or local credentials.
5. Use only the factual overlays above; do not add unverified earnings estimates or market-price claims.
6. Keep total runtime below 60 seconds; recommended cut is ~48 seconds.

## Rights

The package itself contains only original text/layout instructions and cites public source-code screens for factual demonstration. No copyrighted third-party b-roll, logos beyond incidental repository UI, music, or unlicensed imagery is required.